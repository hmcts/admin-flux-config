apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: dev-tenant
  namespace: flux-system
spec:
  interval: 1m0s
  serviceAccountName: dev-tenant
  sourceRef:
    kind: GitRepository
    name: dev-tenant
  prune: true
