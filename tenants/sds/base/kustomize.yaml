apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: tenants
  namespace: flux-system
spec:
  interval: 1m0s
  serviceAccountName: sds-tenant
  sourceRef:
    kind: GitRepository
    name: sds-flux-config
  prune: true
  postBuild:
    substitute:
      TENANT: "sds"